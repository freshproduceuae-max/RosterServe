ROSTERSERVE SESSION HANDOFF
Last updated: 2026-03-28
Project phase: Feature implementation
Repo baseline: Runnable workspace with auth + event + department/sub-team infrastructure; RS-F001, RS-F002, RS-F003 complete and merged to main; RS-F004 plan approved and merged via PR #3

HANDOFF RULE
- Replace this file with the latest durable handoff state.
- Do not append a long diary.
- Keep the "next required action" section precise enough for a fresh agent to continue immediately.

LATEST COMPLETED WORK
- RS-F003 plan file: status updated to Implemented; deviations section added (00004_leader_profile_read.sql and verifyOwnerRole() were not in original plan)
- RS-F001 plan file: confirmed missing from disk — decision made to leave as known gap; no retroactive reconstruction needed
- RS-F004 plan: drafted, Codex-reviewed (addendums applied), approved, merged to main via PR #3

FILES CHANGED (this session, all merged to main)
- docs/plans/plan-rs-f003-department-and-sub-team-structure.md (modified — status: Implemented, deviations section)
- docs/plans/plan-rs-f004-volunteer-onboarding-and-profile-setup.md (new — status: Approved, merged via PR #3)
- docs/tracking/progress.md (RS-F004: approved, next up: implementation)
- docs/tracking/claude-progress.txt (this file)

TEST STATUS
- RS-F004: plan approved — no implementation yet, passes = false
- All RS-F003 checks remain valid (prior session)

KNOWN LIMITATIONS
- RS-F001 plan file is missing from disk — known gap, no action required; future agents read code directly
- No background agents or team tasks were created this session

BLOCKERS
- None

ACTIVE FEATURE
- None (RS-F004 plan approved; implementation not yet started)

NEXT FEATURE
- RS-F004 | Volunteer onboarding and profile setup

NEXT REQUIRED ACTION
- Create branch codex/rs-f004-volunteer-onboarding
- Implement RS-F004 following docs/plans/plan-rs-f004-volunteer-onboarding-and-profile-setup.md exactly
- Re-read the approved plan at the start of the next session before writing any code

BRANCH CONVENTION
- Use codex/<feature-id>-<short-slug>
- RS-F004 branch: codex/rs-f004-volunteer-onboarding

PLAN STATUS
- Scaffolding plan: implemented
- RS-F001 plan: implemented (plan file missing from disk — known gap, no action required)
- RS-F002 plan: implemented and merged
- RS-F003 plan: implemented and merged
- RS-F004 plan: approved (implementation not started)

RS-F004 ARCHITECTURAL DECISIONS (from approved plan)
- Gate location: (app)/layout.tsx adds one redirect for volunteer + !onboarding_complete → /onboarding; middleware stays thin
- Onboarding route group: (onboarding)/ with its own minimal layout — no nav bar, warm-surface wrapper; layout gate ejects non-volunteers and already-onboarded users back to /dashboard
- Three steps: (1) availability preferences — general days/times only, NOT dated blockouts (RS-F005); (2) department interests — FK to departments rows, skippable if none exist; (3) skills — free text, status=pending, approval deferred to RS-F007
- Step data saved incrementally per step (server action per step); partial data persists on browser close
- saveVolunteerSkills is APPEND-ONLY with dedupe — does not replace on re-submit (Codex addendum)
- getActiveDepartmentsForInterests and getOnboardingState must filter departments.deleted_at IS NULL at query layer — stale interest rows from soft-deleted depts are invisible at UI layer (Codex addendum)
- completeOnboarding() sets profiles.onboarding_complete = true then redirects to /dashboard; must be idempotent
- Migration 00005_onboarding.sql: three new tables (availability_preferences, volunteer_interests, volunteer_skills); purely additive, no existing tables altered
- availability_preferences: UNIQUE on volunteer_id (one row per volunteer, upserted)
- volunteer_interests: UNIQUE (volunteer_id, department_id); no status column — that is RS-F006
- volunteer_skills: status column exists from day one (pending/approved/rejected) so RS-F007 can drive it without a migration
- Downstream contracts: RS-F005 extends availability with dated blockouts table; RS-F006 adds status/routing to interests; RS-F007 adds approval workflow UI on top of existing skills table
- Existing volunteers (onboarding_complete=false): gated on first sign-in after deployment — intentional, tested explicitly (validation check #2)
- Design: onboarding is one of the explicit exceptions for font-display and pill CTAs per design-system.md; use brand-warm-500 and bg-surface-warm throughout

CARRY-OVER PRODUCT NOTES
- Event and programme are treated as the same planning unit in v1
- Broad leader read access to all events is closed — visibility follows dept/sub-team ownership (RS-F003)
- Availability should support dated availability plus broader preferences/blockouts (RS-F005 extends RS-F004 foundation)
- Notification delivery remains implementation-neutral between documented options

CANONICAL DOCS
- Vision: VisionDocument_ChurchRosterApp.md
- Design system: docs/design-system/design-system.md
- PRD: docs/prd/prd.md
- Feature registry: docs/features/feature-list.json
- Progress tracker: docs/tracking/progress.md
- Plan rules: docs/plans/README.md

CARRY-OVER EXECUTION NOTES
- Follow the canonical design system for all UI work
- Keep feature execution aligned to the registry order unless an approved plan justifies a dependency-safe exception
- Update progress.md when feature status changes
- Update this file at the end of every implementation or review session
- Mark a feature as passed only when its validation is genuinely satisfied
- ESLint config ignores .next build output and next-env.d.ts
- Pill-shaped CTAs reserved for welcome/promotional moments (onboarding qualifies); not for leader dashboards
- Active/hover states must include non-color indicators (underline, border, etc.) for accessibility
- font-display (Space Grotesk) must be used sparingly — onboarding heading qualifies; dense leader list headings do not
- gh CLI is at /c/Program Files/GitHub CLI/gh.exe (not on PATH in the bash shell)
- Owner role invariants must be enforced at both server-action and DB-trigger layers
- getOwnerDisplayNames(ids) is the correct query for display; getProfilesByRole() is for owner-selection forms only
- Do not commit directly to main — always branch, PR, Codex review, approval, then merge
