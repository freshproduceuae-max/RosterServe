ROSTERSERVE SESSION HANDOFF
Last updated: 2026-04-08 (session 15)
Project phase: Active revision cycle — RS-F001 passed; RS-F002 revision next
Repo baseline: main is clean and up to date; 20 migrations apply cleanly

HANDOFF RULE
- Replace this file with the latest durable handoff state.
- Do not append a long diary.
- Keep the "next required action" section precise enough for a fresh agent to continue immediately.

LATEST COMPLETED WORK

RS-F001 revision — role hierarchy expansion
  PR #17 (plan): docs/prd/prd.md + docs/features/feature-list.json narrowed RS-F001 scope
    - Supporter mirroring deferred to RS-F018
    - Event-creation grant deferred to RS-F002
    - RS-F001 = structural foundation only (enum, types, RLS, dashboard stubs)
  PR #18 (implementation): 2 migrations + full TypeScript + 6 role dashboard stubs
    - Migrations: 00019 (ALTER TYPE ADD VALUE for 3 new roles) + 00020 (data migration, supporter_of column, RLS)
    - 3 Codex blocking review rounds before APPROVED
    - Fixes applied: removed DROP TYPE attempt; fixed event creation gate; cleaned sub_leader identifier remnants
  PR #19 (hotfix): migration split for reset safety
    - Root cause: SQLSTATE 55P04 — PostgreSQL disallows using a new enum value in the same transaction it was added
    - Fix: 00019 contains only ALTER TYPE ADD VALUE; 00020 contains all downstream work

VALIDATION RESULTS (all checks confirmed 2026-04-08)
  - npx supabase db reset: all 20 migrations apply cleanly ✓
  - SELECT policyname ... ILIKE '%sub_leader%': 0 rows ✓
  - npm run typecheck: PASS ✓
  - npm run lint: PASS ✓
  - npm run build: PASS ✓
  - team_head browser: signs in → /dashboard renders, /events visible (no Create CTA), /events/new blocked ✓
  - all_depts_leader browser: signs in → /dashboard stub, /events shows Create CTA, /events/new accessible ✓
  - supporter browser: signs in → /dashboard stub, no Events nav link, 0 JS errors ✓

CODEBASE STATUS
- 20 migrations applied cleanly on db reset
- sub_leader enum value: intentionally left in PostgreSQL type as deprecated dead value (removing it requires dropping all dependent functions/policies; no row carries sub_leader after migration)
- supporter_of column added to profiles (nullable FK) — used by RS-F018 (permission mirroring), not wired yet
- all_depts_leader and supporter dashboard stubs are placeholders; full content is RS-F010's responsibility
- isLeaderRole() excludes supporter (their access is derived from supporter_of FK, not structural scope)
- hasMinimumRole(role, "all_depts_leader") gates event creation — covers super_admin + all_depts_leader

FEATURE STATUS SUMMARY
- RS-F001: passed ✅
- RS-F005: passed ✅
- RS-F002–RS-F004, RS-F006–RS-F010: revision_required
- RS-F011–RS-F018: not_started

BLOCKERS
- None

NEXT REQUIRED ACTION
1. RS-F002 revision: event lifecycle management
   - Restrict event creation to super_admin + all_depts_leader by default (event creation gate already correct in code from PR #18)
   - Add grant mechanism allowing super_admin to grant dept_head / team_head event-creation access
   - Maintain event lifecycle states: draft, published, completed
2. Follow full cycle: write plan -> Codex advisory review -> implement -> Codex blocking code review -> user approval -> validate

BRANCH CONVENTION
- Use codex/<feature-id>-<short-slug>
- Last merged branches: codex/rs-f001-role-hierarchy-revision-impl (PR #18), fix/rs-f001-migration-split (PR #19)
- Next branch: codex/rs-f002-event-lifecycle-revision

PLAN STATUS
- docs/plans/plan-rs-f001-role-hierarchy-revision.md: implemented; validation outstanding
- All pre-revision plans (RS-F002 through RS-F010) are outdated — write new revision plans

CARRY-OVER EXECUTION NOTES
- Follow the canonical design system for all UI work
- Permanent membership persists across events — never reset by event-level actions
- Team Head is optional; Dept Head is always required for a volunteer
- Auto-suggestions never auto-assign — Dept Head confirmation always required
- Do not commit directly to main; use a clean branch per feature
- The sub_leader enum value is deprecated dead; do not introduce it into new code or policies

CANONICAL DOCS
- Vision: VisionDocument_ChurchRosterApp.md (revised 2026-04-07)
- PRD: docs/prd/prd.md (revised 2026-04-07, RS-F001 scope narrowed 2026-04-08)
- Feature registry: docs/features/feature-list.json (v2; RS-F001 in_review pending browser validation)
- Progress tracker: docs/tracking/progress.md
- Session handoff: docs/tracking/claude-progress.txt
