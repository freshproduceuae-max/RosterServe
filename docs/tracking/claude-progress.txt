ROSTERSERVE SESSION HANDOFF
Last updated: 2026-03-30 (session 7)
Project phase: Feature implementation
Repo baseline: Runnable workspace with auth + event + department/sub-team + volunteer onboarding + availability/blockout + interest request infrastructure; RS-F001–RS-F006 complete and merged to main; all 6 features passed

HANDOFF RULE
- Replace this file with the latest durable handoff state.
- Do not append a long diary.
- Keep the "next required action" section precise enough for a fresh agent to continue immediately.

LATEST COMPLETED WORK
- RS-F007: Skill profile and approval — implementation complete (2026-03-30)
  - Migration 00009, lib layer (types, queries, actions), 7 UI components, /skills page, nav link, seed examples
  - typecheck, lint, build all pass
  - PR created for review (see NEXT REQUIRED ACTION)
  - 18-item manual validation not yet completed

FILES CHANGED (session 7, on branch codex/rs-f007-skill-profile-approval)
- supabase/migrations/00009_skills.sql (new)
- apps/web/lib/skills/types.ts (new)
- apps/web/lib/skills/queries.ts (new)
- apps/web/lib/skills/actions.ts (new)
- apps/web/app/(app)/skills/page.tsx (new)
- apps/web/app/(app)/skills/_components/claim-skill-form.tsx (new)
- apps/web/app/(app)/skills/_components/skill-claim-list.tsx (new)
- apps/web/app/(app)/skills/_components/volunteer-skills-view.tsx (new)
- apps/web/app/(app)/skills/_components/department-skill-catalog.tsx (new)
- apps/web/app/(app)/skills/_components/skill-claim-card.tsx (new)
- apps/web/app/(app)/skills/_components/leader-skills-view.tsx (new)
- apps/web/app/(app)/skills/_components/super-admin-skills-view.tsx (new)
- apps/web/app/(app)/skills/_components/status-badge.tsx (new — shared StatusBadge)
- apps/web/app/(app)/app-nav.tsx (modified — Skills link added)
- supabase/seed.sql (modified — RS-F007 examples appended)
- docs/tracking/progress.md (modified — RS-F007 status in_review)

TEST STATUS
- RS-F001–RS-F006: all passed
- RS-F007: implementation complete; typecheck/lint/build pass; PR in review; manual validation (18 items) pending

KNOWN LIMITATIONS
- RS-F001 plan file is missing from disk — known gap, no action required
- RS-F004 manual validation (11 checks) was not independently confirmed — acknowledged gap from prior session
- RS-F006 plan was committed directly to main (not via PR) — known process deviation, acknowledged
- npx supabase db reset not verified (Docker not running during implementation) — migration SQL verified statically

WORKSPACE STATE
- Branch: codex/rs-f007-skill-profile-approval
- apps/web/tsconfig.tsbuildinfo — modified (auto-generated build artefact; not feature work; safe to ignore)
- .claude/settings.local.json — untracked (Claude Code local config; not feature work; do not commit)

BLOCKERS
- None

ACTIVE FEATURE
- RS-F007 | Skill profile and approval (P0) — implementation in PR review

NEXT REQUIRED ACTION
1. Human approves the RS-F007 implementation PR
2. Merge to main, delete branch
3. Complete 18-item manual validation (plan §Validation Plan in docs/plans/plan-rs-f007-skill-profile-approval.md)
4. On all 18 items passing:
   - Update docs/tracking/progress.md: RS-F007 status → passed
   - Update docs/features/feature-list.json: RS-F007 passes → true
   - Update docs/plans/plan-rs-f007-skill-profile-approval.md: Status → Implemented and Validated
   - Update this file
   - Commit directly to main (tracking-only changes)
5. Begin RS-F008 plan drafting (Roster planning and assignment management, P0)

BRANCH CONVENTION
- Use codex/<feature-id>-<short-slug>
- RS-F008 branch: codex/rs-f008-roster-planning

PLAN STATUS
- Scaffolding plan: implemented
- RS-F001 plan: implemented (plan file missing from disk — known gap)
- RS-F002 plan: implemented and merged
- RS-F003 plan: implemented and merged
- RS-F004 plan: implemented and merged
- RS-F005 plan: implemented, merged, and validated
- RS-F006 plan: implemented, merged, and validated
- RS-F007 plan: IMPLEMENTED (docs/plans/plan-rs-f007-skill-profile-approval.md) — PR in review

RS-F007 KEY IMPLEMENTATION NOTES (for validation session)
- getSkillClaimsForScope() (renamed from getPendingSkillClaimsForScope) returns ALL statuses — leader view renders pending + reviewed rows
- status-badge.tsx is a shared component imported by both skill-claim-card.tsx and skill-claim-list.tsx
- Legacy onboarding skills (NULL department_id): visible in volunteer view with "Legacy — no department link" muted label; withdrawable; absent from leader/admin views
- Soft-deleted catalog skill: volunteer_skills.skill_id FK NOT nulled (soft-delete doesn't cascade); claim remains valid using stored name column
- Super-admin view: readOnly={true} passed to all SkillClaimCard instances — no action buttons shown
- SkillClaimCard.handleReject resets setRejecting(false) on success

RS-F006 NOTABLE IMPLEMENTATION DECISIONS (carry-forward reference)
- Explicit role dispatch in /interests page — no isLeaderRole(); sub_leader redirects to /dashboard
- Partial unique index excludes rejected rows (WHERE deleted_at IS NULL AND status != 'rejected') — allows re-application after rejection
- Immutable field trigger (volunteer_interests_lock_core_fields) locks volunteer_id and department_id at DB level
- Volunteer withdraw WITH CHECK requires deleted_at BETWEEN (now() - 30s) AND (now() + 30s) — prevents arbitrary audit timestamp
- Dept_head review USING requires status = 'pending' — no re-review/reversal at DB layer
- getVolunteersInScope() and getBlockoutsForScope() both role-aware: super_admin skips interest filter, dept_head applies it

CARRY-OVER PRODUCT NOTES
- Event and programme are treated as the same planning unit in v1
- Broad leader read access to all events is closed — visibility follows dept/sub-team ownership (RS-F003)
- Availability supports dated blockouts (RS-F005) plus general preferences (RS-F004)
- Notification delivery remains implementation-neutral between documented options

CANONICAL DOCS
- Vision: VisionDocument_ChurchRosterApp.md
- Design system: docs/design-system/design-system.md
- PRD: docs/prd/prd.md
- Feature registry: docs/features/feature-list.json
- Progress tracker: docs/tracking/progress.md
- Plan rules: docs/plans/README.md

CARRY-OVER EXECUTION NOTES
- Follow the canonical design system for all UI work
- Keep feature execution aligned to the registry order unless an approved plan justifies a dependency-safe exception
- Update progress.md when feature status changes
- Update this file at the end of every implementation or review session
- Mark a feature as passed only when its validation is genuinely satisfied
- ESLint config ignores .next build output and next-env.d.ts
- Pill-shaped CTAs reserved for welcome/promotional moments; not for dashboard or management pages
- Active/hover states must include non-color indicators (underline, border, etc.) for accessibility
- font-display (Space Grotesk) used sparingly — dense management pages use DM Sans
- gh CLI is at /c/Program Files/GitHub CLI/gh.exe (not on PATH in the bash shell)
- Owner role invariants must be enforced at both server-action and DB-trigger layers
- getOwnerDisplayNames(ids) is the correct query for display; getProfilesByRole() is for owner-selection forms only
- Do not commit directly to main — always branch, PR, Codex review, approval, then merge
- Zod error property is .issues (not .errors) — this bit us in RS-F005 actions.ts
- "Generate a PR" after plan drafting = plan-only PR for human review; never an instruction to start implementation
