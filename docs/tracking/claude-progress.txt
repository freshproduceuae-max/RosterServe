ROSTERSERVE SESSION HANDOFF
Last updated: 2026-04-08 (session 16)
Project phase: Active revision cycle — RS-F001 + RS-F002 passed; RS-F003 revision next
Repo baseline: main is clean and up to date; 23 migrations apply cleanly

HANDOFF RULE
- Replace this file with the latest durable handoff state.
- Do not append a long diary.
- Keep the "next required action" section precise enough for a fresh agent to continue immediately.

LATEST COMPLETED WORK

RS-F002 revision — event lifecycle management
  PR #20 (implementation): event creation grants
    - can_create_events boolean NOT NULL DEFAULT false added to profiles (migration 00021)
    - all_depts_leader events SELECT policy added (migration 00021)
    - events INSERT and UPDATE RLS policies expanded to cover all_depts_leader by default
      and granted dept_head / team_head (migration 00021)
    - canManageEvents(profile) + canManageThisEvent(profile, event) helpers in lib/auth/roles.ts
    - grantEventCreation / revokeEventCreation server actions in lib/events/grants.ts
    - /events/grants page (super_admin only) with GrantList toggle component
    - All four event actions (createEvent, updateEvent, transitionEventStatus, softDeleteEvent)
      updated to use canManageEvents check
    - events/page.tsx, events/new/page.tsx, events/[id]/page.tsx, events/[id]/edit/page.tsx updated
    - EventDetailCard isSuperAdmin prop renamed to canManage; sourced from canManageThisEvent

  PR #21 (validation fixes):
    - Migration 00022: super_admin UPDATE policy on profiles (dept_head / team_head rows)
      Root cause: grantEventCreation / revokeEventCreation silently wrote 0 rows because no
      RLS UPDATE policy allowed super_admin to update another user's profile row
    - Migration 00023: "Granted users can read events they created" SELECT policy on events
      Root cause: createEvent's .select("id").single() failed after INSERT because the new event
      has no departments yet and the dept_head SELECT policy is scoped to owned-dept events only
    - EventDetailCard dialog crash fixed: transitionAction and deleteAction wrapped in
      startTransition(); immediate setTransitionTarget(null) / setShowDelete(false) calls removed
      Root cause: useActionState dispatch called imperatively without startTransition + modal
      unmounted before async action resolved → React removeChild null crash

VALIDATION RESULTS (checks 5–16 confirmed 2026-04-08)
  - Check 5: super_admin creates event → appears in list ✓
  - Check 6: all_depts_leader creates event → appears in list ✓
  - Check 7: dept_head (no grant) → no Create CTA at /events ✓
  - Check 8: dept_head (no grant) → /events/new redirects to /events ✓
  - Check 9: super_admin grants dept_head at /events/grants → button flips to Revoke ✓
  - Check 10: granted dept_head → CTA visible, event creation succeeds ✓
  - Check 11: granted dept_head → Edit/action on own event; absent on super_admin's event ✓
  - Check 12: revoke grant → CTA gone, /events/new blocks again ✓
  - Check 13: draft→published→completed chain valid; published→draft rejected by DB trigger ✓
  - Check 14: can_create_events NOT NULL DEFAULT false confirmed at schema level ✓
  - Check 15: all_depts_leader sees all active events regardless of department ownership ✓
  - Check 16: /events/[id]/edit accessible for own event; redirects for non-owned event ✓
  - npm run typecheck: PASS ✓
  - npm run lint: PASS ✓

CODEBASE STATUS
- 23 migrations applied cleanly on db reset
- sub_leader enum value: deprecated dead value; no row carries sub_leader; do not use in new code
- supporter_of column on profiles (nullable FK) — wired by RS-F018, not yet active
- all_depts_leader and supporter dashboard stubs are placeholders; full content is RS-F010's responsibility
- isLeaderRole() excludes supporter (access is derived from supporter_of FK, not structural scope)
- canManageEvents(profile) gates event creation; canManageThisEvent(profile, event) gates edit/action surface

FEATURE STATUS SUMMARY
- RS-F001: passed ✅
- RS-F005: passed ✅
- RS-F002: passed ✅
- RS-F003–RS-F004, RS-F006–RS-F010: revision_required
- RS-F011–RS-F018: not_started

BLOCKERS
- None

NEXT REQUIRED ACTION
1. RS-F003 revision: department and team structure
   - Write plan → Codex advisory review → implement → Codex blocking review → user approval → validate
   - Likely scope: department/sub-team CRUD scoped to dept_head ownership, team_head assignment
   - Plan file path: docs/plans/plan-rs-f003-<slug>.md (to be created)

BRANCH CONVENTION
- Use feat/<feature-id>-<short-slug>
- Last merged branches: feat/rs-f002-event-lifecycle-revision (PR #21)
- Next branch: feat/rs-f003-<slug>

PLAN STATUS
- docs/plans/plan-rs-f001-role-hierarchy-revision.md: implemented and validated ✓
- docs/plans/plan-rs-f002-event-lifecycle-revision.md: implemented and validated ✓ (two RLS deviations documented)
- All other pre-revision plans (RS-F003 through RS-F010) are outdated — write new revision plans

CARRY-OVER EXECUTION NOTES
- Follow the canonical design system for all UI work
- Permanent membership persists across events — never reset by event-level actions
- Team Head is optional; Dept Head is always required for a volunteer
- Auto-suggestions never auto-assign — Dept Head confirmation always required
- Do not commit directly to main; use a clean branch per feature
- The sub_leader enum value is deprecated dead; do not introduce it into new code or policies
- canManageEvents / canManageThisEvent are the canonical event-access helpers — do not revert to raw role checks
- DepartmentListSection still uses isSuperAdmin (not canManageEvent) — department management is RS-F003 scope

CANONICAL DOCS
- Vision: VisionDocument_ChurchRosterApp.md (revised 2026-04-07)
- PRD: docs/prd/prd.md (revised 2026-04-07)
- Feature registry: docs/features/feature-list.json (v2; RS-F001 + RS-F002 passed)
- Progress tracker: docs/tracking/progress.md
- Session handoff: docs/tracking/claude-progress.txt
